<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Hello World</title>
</head>
<body>
    {{ $people := .People }}
    <ul>
        {{ range $people }}
            <li>{{ .Name }} is from {{ .Country }}</li>
        {{ end }}
    </ul>
    <hr>
        {{ $cars := .Cars }}
        <ul>
            {{ range $cars }}
                <li>
                    {{ .Name }} is made by {{ .Manufacturer }} and is {{ .Age }} years old.
                </li>
            {{ end }}
        </ul>
</body>
</html>